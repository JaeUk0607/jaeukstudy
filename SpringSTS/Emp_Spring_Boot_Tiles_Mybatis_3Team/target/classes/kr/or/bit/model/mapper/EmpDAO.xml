<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.bit.model.EmpDAO">
	 <!-- insert -->
	 <!-- <parameterMap type="hashmap" id="prameterMap">
	 	<parameter property="empno" mode="IN" jdbcType="NUMBER" javaType="INTESER"/>
	 	<parameter property="ename" mode="IN" jdbcType="VARCHAR" javaType="String"/>
	 	<parameter property="job" mode="IN" jdbcType="VARCHAR" javaType="String"/>
	 	<parameter property="mgr" mode="IN" jdbcType="NUMBER" javaType="INTESER"/>
	 	<parameter property="hiredate" mode="IN" jdbcType="VARCHAR" javaType="String"/>
	 	<parameter property="sal" mode="IN" jdbcType="NUMBER" javaType="INTESER"/>
	 	<parameter property="comm" mode="IN" jdbcType="NUMBER" javaType="INTESER"/>
	 	<parameter property="deptno" mode="IN" jdbcType="NUMBER" javaType="INTESER"/>
	 </parameterMap> -->
	 <insert id="insertEmp" statementType="CALLABLE" parameterType="kr.or.bit.model.EmpDTO">
	   {call insert_emp (#{empno},#{ename} ,#{job} ,#{mgr} ,#{hiredate} ,#{sal} ,#{comm} , #{deptno}, #{msg, mode=OUT, jdbcType=VARCHAR})}
	  
	  
	  <!--    <insert id="insertEmp" statementType="CALLABLE" parameterType="kr.or.bit.model.EmpDTO"> -->
	  <!-- insert into emp values(#{empno},#{ename} ,#{job} ,#{mgr} ,#{hiredate} ,#{sal} ,#{comm} , #{deptno}) -->
	 </insert>
	 <!-- update -->
	 <update id="updateEmp" parameterType="kr.or.bit.model.EmpDTO">
	  update emp 
	  set ename = #{ename} , job = #{job} ,   mgr = #{mgr} , hiredate = #{hiredate} , sal = #{sal} ,comm = #{comm} ,  deptno = #{deptno} 
	  where  empno=#{empno}
	 </update>
	 <!-- delete-->
	 <delete id="deleteEmp" parameterType="Integer">
	  delete from emp  where  empno=#{empno}
	 </delete>
	 <!-- List(전체조회) -->
	 <select id="getEmpList" resultType="kr.or.bit.model.EmpDTO">
	       select * from emp
	 </select>
	 <!-- 단일 조회 -->
	<!-- 단일 조회 -->
    <resultMap id="empMap" type="kr.or.bit.model.EmpDTO"/>
    <select id="getEmp" statementType="CALLABLE" parameterType="hashMap">
           {call proc_EmpList(#{empno}, #{empList, mode=OUT, jdbcType=CURSOR, javaType=java.sql.ResultSet, resultMap=empMap})}
    </select>
</mapper>